# План разработки «Геймификация для ВСМ»

## Этап 12 — пакет сдачи хакатона

Цель: жюри запускает рабочее демо из чистого checkout и пустой БД по README.
Продуктовая логика этапов 1–11 сохраняется.

- [x] `compose.yaml`, `.env.example`: одноразовый bootstrap применяет Alembic,
      импортирует JSON и публикует demo challenges до старта backend/worker;
      отдельные host-порты позволяют проверить чистый проект без удаления рабочей БД.
- [x] README: 13 разделов для запуска, проверки и расширения;
      ARCHITECTURE/API/SCENARIO_ENGINE сверены с кодом. Новые USER_FLOW,
      DEMO_SCRIPT, LIMITATIONS; component, decision sequence и user-flow Mermaid.
- [x] Чистый Compose: новые контейнеры и том, readiness, каталог, demo-вход,
      браузерное прохождение через Nginx, debrief, worker; повторная инициализация
      сохраняет версии, награды и окна кампаний. E2E могут обращаться к готовому Compose.
- [x] Проверки backend/frontend, ссылки документации, diff и итоговое ревью;
      завершение: один commit `docs: complete deployment and architecture package`, push и остановка.

Критерии проверки: пустая БД не требует ручных миграций; ошибка bootstrap
не допускает старт приложения; повтор запуска не сбрасывает результаты;
README явно различает demo identity и пароль БД; локальный запуск передаёт
окружение каждому процессу. Production-развёртывание в этот этап не входит.

## Этап 11 — hardening

- Проверены synthetic-персоны/контент, отсутствие отслеживаемого `.env`,
  известные шаблоны секретов и совпадения локальных секретов в Git-истории.
- API: лимит тела 64 KiB, no-store/nosniff, отказ от управляющих символов
  и ревизий вне диапазона integer; общий безопасный JSON ошибок.
- Миграция `20260926_06`: согласованность JSON-снимка и строки сессии,
  ревизия/длина истории, типы наград и XP ≤ 130 для rule_version 1,
  SHA-256 хеши токенов, непустые ключи старта.
- Расширены unit/engine/API/PostgreSQL/frontend-тесты: подделки эффектов,
  баллов, владельца и времени; невозможные переходы; восстановление после
  отказа; конкурентные терминальные повторы; гонка timeout/HTTP; недоверенный текст.
- Playwright happy-path: выбор ситуации → два решения → завершение → debrief
  → восстановление после reload, с проверкой отсутствия баллов в командах.
- [SECURITY.md](../SECURITY.md) фиксирует модель доверия и ограничения демо.
  Реальная аутентификация, rate limiting и production-эксплуатация не входят
  в этот этап. После проверок предусмотрен один commit/push и остановка.

## Этап 10 — реализован

- Чистые правила челленджей поверх журналов завершённых попыток: разные точные
  версии сценариев, фиксированное серверное окно, safety ≥ 50, без timeout.
- Миграция `20260926_05`: challenges, challenge_scenarios, notifications.
  Дедупликация событий по получателю, персональное read/unread, expiration.
- API `/api/v1/challenges`, `/notifications`, PUT `/notifications/{id}/read`.
  Worker сверяет события раз в минуту, API восстанавливает пропуски после простоя.
- Две новые синтетические ситуации: помощь при посадке и забытая вещь.
  Явная повторяемая публикация суточного и недельного демо-челленджей.
- Экран «События» в визуальном языке приложения: прогресс, сроки, переход
  к ситуации, входящие и история; доступен вне активной игровой сцены.
- Проверки: доменные границы времени, PostgreSQL/HTTP, конкурентная дедупликация,
  read/unread, истечение, сохранение окон, unit UI и браузерный путь.
- Подробности: [RETENTION.md](RETENTION.md). Внешняя доставка, управление
  кампаниями через UI и следующий этап остаются за рамками.

## Реализованный scope этапов 2–9 — обучающий симулятор

Новое задание владельца на этап 2 задаёт обучающую платформу ВСМ и EmployeeProfile.
Оно уточняет scope для текущей работы: проектируется исходный учебный домен.
Прежние ограничения этапа 1 сохранены ниже как история решения; пассажирская
геймификация не добавляется. Архитектура и диаграмма — в [ARCHITECTURE.md](ARCHITECTURE.md).

### Реализовано

- Этап 1: React/Vite, FastAPI health/readiness, PostgreSQL, Docker Compose,
  Alembic, конфигурация, pytest и Vitest.
- Этап 2: `backend/app/domain/` — scenario, gameplay/session, scoring,
  competencies, achievements, profiles, analytics и notifications.
- Типы EmployeeProfile, Competency, Scenario, ScenarioNode, Choice,
  ScenarioSession, Decision, Achievement, AchievementUnlock, Notification.
- Валидация графа и снимков сессии, DSL v1 условий/эффектов без eval/exec,
  неизменяемые значения, чистые расчёты и unit-тесты независимости от web/ORM.
- Этап 3: строгий JSON/Pydantic-контракт, преобразование в домен, отдельная
  timeout-ветка, объяснения последствий и настраиваемый запрет циклов.
- PostgreSQL JSONB для версий сценариев, Alembic-миграция, транзакционный импорт
  и чтение конкретной версии; повторный импорт одинакового документа безопасен.
- Три небольших синтетических демо и JSON Schema для редактора. Проверки
  валидации, CLI, round-trip хранения, конфликтов версий и миграций.
- Этап 4: чистый `app/domain/engine.py` — start, текущий узел, доступные
  варианты, условия, эффекты, переходы, таймауты, завершение и история.
- Закрепление версии сценария, явные начальные баллы и время, проверка
  ожидаемого номера команды, повторы решений без повторных начислений.
- Восстановление снимка повторным исполнением истории и отдельный строгий
  JSON/Pydantic-адаптер сессии; домен зависит только от стандартной библиотеки.
- [SCENARIO_ENGINE.md](SCENARIO_ENGINE.md): state machine, порядок обработки,
  запускаемый пример, таймеры, ограничения и добавление ветки через JSON.
- Этап 5: независимые Loyalty/Safety, начальные значения 50/50 в application
  service, границы 0–100 и закреплённая в попытке политика. Эффекты одной метрики
  суммируются, затем применяется clamp и сохраняется объяснимый журнал.
- `scenario_sessions`, миграция `20260926_02`, JSONB-снимок формата 2 с историей
  и журналом, ревизия и индекс deadline. Формат 1 явно отклоняется.
- Минимальный API start/get/decide, авторитетное время PostgreSQL после
  блокировки строки, атомарное сохранение решения и состояния, повторы без
  двойного начисления и приоритет таймаута при `now >= deadline`.
- Отдельный `timer-worker`: автоматические таймауты без браузера, восстановление
  обработки после рестарта по сохранённым deadline и `FOR UPDATE SKIP LOCKED`.
- [GAME_MECHANICS.md](GAME_MECHANICS.md): правила, HTTP-примеры, worker,
  конкурентные команды, ограничения локального запуска.

### Выполненный порядок этапа 3

1. Зафиксировать строгую схему и тесты ошибок. Внешний адаптер использует
   Pydantic, домен сохраняет зависимость только от стандартной библиотеки.
2. Добавить JSONB-таблицу, миграцию и репозиторий с транзакцией у вызывающей стороны.
   JSONB сохраняет граф целиком; отдельные таблицы узлов усложнили бы импорт
   и версионирование без пользы для текущего сценария чтения.
3. Добавить синтетический контент, команды validate/import/export-schema и
   документацию редактирования. Конфликт содержимого требует новой версии.
4. Проверить PostgreSQL в изолированных тестовых схемах, upgrade/downgrade,
   форматирование, lint, typecheck, весь pytest и существующий frontend.
5. Просмотреть diff, commit `feat: add scenario schema and persistence`, push
   текущей ветки и остановиться.

### Выполненный порядок этапа 4

1. Зафиксировать контракт чистого движка и тесты ветвления, условий,
   недопустимого перехода, повторов и восстановления.
2. Реализовать start/advance/expire, запросы текущего состояния и проверку
   истории; сохранить независимость от HTTP, ORM, UI, часов и генерации ID.
3. Добавить строгий JSON-снимок через внешний адаптер, не создавая таблицы
   сессий или API. Проверить закрепление версии и целостность восстановления.
4. Описать state machine, обработку команды, таймеры и расширение контента;
   проверить пример документации, Ruff, mypy, pytest и независимость домена.
5. Просмотреть итоговый diff и остановиться в границах этапа. Хранение сессий,
   API прохождения и игровой интерфейс требуют следующего задания.

### Выполненный порядок этапа 5

1. Добавить независимые показатели, min/max, однократный clamp после суммы
   эффектов и журнал с исходным значением, запрошенной дельтой, итогом и объяснением.
2. Закрепить политику в сессии, сериализовать её и журнал в снимке формата 2;
   проверять результат повторным исполнением истории, явно отклонять формат 1.
3. Добавить миграцию, репозиторий сессий и use cases start/get/decide/expire_due.
   Блокировать строку до получения времени БД; сохранять timeout до ответа 409.
4. Запустить независимый worker в Compose, добавить минимальный API и проверить
   реальные PostgreSQL-гонки, двойную отправку, рестарт и позднюю обработку.
5. Обновить документацию, проверить Ruff, mypy, pytest, миграции, Compose
   и существующий frontend; просмотреть diff, создать commit
   `feat: add timed decisions and dual scoring`, отправить текущую ветку и остановиться.

### Выполненный этап 6

- `/api/v1` и OpenAPI: demo auth, profiles, scenarios, sessions, decisions,
  results, achievements, leaderboard и analytics; schemas и единые ошибки.
- Серверный владелец из bearer-токена, сохранённые хеши demo-токенов с TTL,
  проверка доступа к попыткам и атомарный идемпотентный старт.
- Пагинация коллекций; итоги и статистика по сохранённым снимкам, независимые
  рейтинги лучших завершённых попыток в рамках версии сценария.
- Миграция `20260926_03`: profiles, tokens, start keys, achievement definitions/unlocks.
- HR/LMS TrainingResultExport v1: JSON Schema и endpoint, возвращающий 501
  после успешной валидации, без отправки и сохранения.
- [API.md](API.md): решения и план этапа, маршруты, запуск и ограничения.
- Проверки API на реальном PostgreSQL, quality checks, Docker/Nginx smoke;
  commit `feat: expose gameplay and integration API`, push и остановка.

### Выполненный этап 7

- Основной Scenario Runner: выбор ситуации, реплика персонажа, контекст, действия,
  серверный таймер, независимые шкалы, маршрут и разбор решений.
- Адаптивная рабочая сцена, клавиатура, фокус, reduced motion и сетевые состояния.
- Клиент `/api/v1`: строгая проверка ответов, demo-вход, sessionStorage,
  восстановление попытки и повтор исходной команды после потерянного ответа.
- Vitest и Playwright с реальным backend: прохождение, серверный таймаут,
  повтор без начисления, перезагрузка, мобильная ширина и клавиатура.
- [SCENARIO_RUNNER.md](SCENARIO_RUNNER.md): визуальные решения, ответственность,
  восстановление, запуск и ограничения. Полный профиль и лидерборд отложены до этапа 8.
- Commit `feat: build immersive scenario runner`, push текущей ветки и остановка.

### Выполненный этап 8

- Чистые правила XP, уровня, накопленных компетенций и четырёх поведенческих
  достижений; движок сценариев сохраняет независимость от интерфейса и хранилища.
- Миграция `20260926_04`: принадлежность подразделению, журнал наград
  `session_rewards`, определения поведенческих достижений без фиктивных результатов.
- Атомарное начисление при завершении через решение или worker, сериализация
  наград профиля, backfill проверенных прошлых результатов и постоянные unlock.
- REST прогресса и рейтингов бригады/депо/компании с серверной фильтрацией и
  пагинацией. Разрешённый список синтетических персон для демонстрации результатов.
- Учебный паспорт, шкала уровня, журнал достижений, табло рейтинга и награда
  в разборе; клавиатура, мобильная версия, loading/error/retry и смена персоны.
- [GAMIFICATION.md](GAMIFICATION.md): правила, границы, реализация и проверки.
  Unit/API/PostgreSQL и браузерные тесты начислений, гонок и полного цикла.
- Commit `feat: add conductor gamification system`, push текущей ветки и остановка.

### Выполненный этап 9

- Детерминированный разбор проверенной истории: решение, последствие, причины
  изменения обеих шкал, компетенции, доступность альтернатив и рекомендации.
- Сравнение непосредственных эффектов с учётом условий до выбора и clamp;
  компромиссы не сводятся к универсальному правильному ответу.
- Аналитика прогресса, наблюдений навыков, сильных сторон/зон развития,
  повторяющихся проблем и статистики сценариев с разделением версий.
- Структурированные события решений остаются в атомарно сохранённом снимке v2;
  существующие данные доступны без миграции или повторного прохождения.
- REST debrief и competency analytics с ownership и прежним форматом ошибок.
- Временная линия разбора, альтернативы и аналитический раздел учебного паспорта;
  адаптивность, клавиатура, reduced motion и повтор загрузки при сетевой ошибке.
- [DEBRIEF_ANALYTICS.md](DEBRIEF_ANALYTICS.md): правила v1, ограничения и проверки.
  Unit, PostgreSQL integration и Playwright полного пути; без runtime LLM.
- Commit `feat: add competency analytics and debrief`, push и остановка.

### Следующие этапы (ещё не реализованы)

- Продуктовая балансировка, антифрод и внешняя доставка уведомлений.
- Методическая валидация учебных рекомендаций и порогов аналитики специалистами.
- Production-аутентификация/роли, read models для больших объёмов,
  эксплуатация и интеграции с конкретными согласованными HR/LMS системами.

Следующий этап не начинается без отдельного задания. Демонстрационный вход
использует синтетические персоны, не подтверждает личность сотрудника.

## История этапа 1: PRODUCT SCOPE — pending clarification

Ниже сохранено прежнее решение, действовавшее до нового задания на этап 2.
Фразы о незафиксированном scope и запрете доменных типов относятся к этапу 1.

Продуктовый scope НЕ ЗАФИКСИРОВАН. Исходное официальное ТЗ описывает обучающий
симулятор для проводников, материалы QA-сессии — пассажирскую геймификацию.
Владелец репозитория должен уточнить целевую аудиторию и продуктовый scope.
До этого архитектура не закрепляет ни проводника, ни пассажира как конечного
пользователя. Следующий этап без отдельного задания не начинается.

Материалы QA-сессии обсуждают паспорт путешественника, достижения за реальные
действия и поездки, бонусную валюту «Вёрсты», челленджи, событийную модель
начисления, offline event queue, antifraud и ненавязчивую интеграцию геймификации
в основной интерфейс. Это контекст противоречия, а не утверждённые требования.

Не создаются EmployeeProfile, PassengerProfile, Scenario Engine, Miles/«Вёрсты»,
Achievements, Loyalty Program, HR/LMS integration и ticketing integration.
Доменных таблиц, миграций и бизнес-модулей на первом этапе нет.

## Исходное назначение (сохранено; не утверждённый scope)

Обучать проводников ВСМ-400 принятию решений в рабочих ситуациях через
нелинейные сценарии, ограничение времени и разбор последствий.

## Подтверждённый технический фундамент: архитектура и границы

Один репозиторий: `frontend/` (React, TypeScript, Vite), `backend/` (FastAPI,
Pydantic, SQLAlchemy, Alembic), `scenarios/` (JSON Schema и синтетические демо), `infra/`
(Nginx), `docs/`. PostgreSQL — постоянное хранилище.

На этапе 1 frontend отображал состояние подключения. На этапе 7 он проходит
сценарии через REST API, восстанавливает попытку и показывает серверный результат.
На этапе 8 отображает серверный прогресс профиля, награду и рейтинги подразделений.
На этапе 9 показывает обучающий разбор и аналитику компетенций из серверных фактов.
Backend предоставляет health/readiness и проверяет соединение с PostgreSQL.
Доступ к БД только через backend. Контракт HTTP описан OpenAPI;
в разработке Vite проксирует `/api`,
в контейнерах ту же роль выполняет Nginx. Для `/api/v1/` путь сохраняется;
старый технический `/api/health` проксируется с удалением `/api`.

## Этап 1: технический фундамент

Порядок реализации и проверки:

1. Создать тесты HTTP-контракта и состояний экрана подключения.
2. Реализовать `backend/app/main.py`: `GET /health` → `{"status":"ok"}`;
   `GET /ready` проверяет PostgreSQL и возвращает 503 при недоступности.
   Добавить SQLAlchemy metadata и конфигурацию Alembic без доменных таблиц.
3. Реализовать `frontend/src/App.tsx`: проверка `/api/health`, состояния ожидания,
   успеха, ошибки, таймаут и повторная проверка. Проверять формат ответа.
4. Настроить Dockerfiles, Compose, Nginx и переменные окружения без секретов.
5. Выполнить форматирование, lint, typecheck, pytest, Vitest, сборку frontend,
   проверку Compose и доступные интеграционные проверки. Просмотреть diff,
   создать один bootstrap-коммит и отправить текущую ветку.

Критичные проверки: сетевой сбой, неверный JSON/контракт, HTTP-ошибка, зависший
запрос, недоступная БД. Health — признак работы процесса, не готовности БД.

## Исходные продуктовые предположения (сохранены; приостановлены)

Следующий текст сохраняет первоначальное описание границ и этапов. Он не
определяет действующую архитектуру и не разрешает реализацию бизнес-модулей.
После уточнения владельцем scope потребуется отдельное согласование плана.

Frontend отвечает за отображение, ввод и состояния запросов. Backend владеет
правилами сценариев, валидацией решений, временем, очками и сохранением прогресса.
Клиент не определяет итоговые баллы и соблюдение таймера.

### Исходные следующие этапы (не реализованы; приостановлены)

2. Доменные модели и миграции: пользователь, профиль, компетенция, сценарий,
   версия сценария, узел, вариант выбора, попытка, решение, изменение баллов.
   Утвердить JSON-схему контента и правила публикации версий.
3. Движок сценариев: ветвление, диалоги, серверные сроки критических решений,
   идемпотентная запись решения, восстановление попытки после разрыва связи.
4. Игровой интерфейс: Passenger Loyalty, Safety Rating, компетенции,
   достижения, уровни и лидерборд. Сначала утвердить правила начисления баллов.
5. Разбор решений и аналитика компетенций; уведомления и настройки доставки.
6. Аутентификация и роли, защита персональных данных, API для HR/LMS,
   аудит и эксплуатация. Контракты интеграций согласовать с реальными системами.

Для этапа 3 добавить Playwright и E2E-проверки прохождения, истечения таймера,
повторной отправки решения и восстановления сессии. Пока нет соответствующих
пользовательских сценариев, Playwright не устанавливается.

## Подтверждённые технические решения

- Синхронный SQLAlchemy и psycopg: достаточно для первого этапа, без лишнего
  асинхронного слоя. Alembic управляет схемой; автоматического create_all нет.
- Pydantic фиксирует HTTP-ответы. Liveness не зависит от конфигурации БД.
- Секреты только в окружении; `.env` исключён из Git и Docker context.
- В контейнерах БД ждёт healthcheck, backend проверяет readiness, frontend
  доступен после готовности backend. Схему обновлять отдельной командой Alembic.
- Тесты: pytest для backend, Vitest и Testing Library для frontend.
- Docker Compose предназначен для локальной разработки, не production:
  аутентификация, TLS, резервное копирование и лимиты — отдельный этап.
